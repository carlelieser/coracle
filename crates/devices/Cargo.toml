[package]
name = "coracle-devices"
description = "virtio-mmio devices, GICv2, generic timer, PL011, PL031."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[features]
default = ["threads"]
threads = ["coracle-core/threads"]

[dependencies]
coracle-core = { workspace = true, default-features = false }

[lints]
workspace = true
