[package]
name = "coracle-machine"
description = "QEMU `virt` machine model: wiring, device tree, machine loop."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[features]
default = ["threads"]
threads = ["coracle-core/threads", "coracle-devices/threads"]

[dependencies]
coracle-core = { workspace = true, default-features = false }
coracle-devices = { workspace = true, default-features = false }

[lints]
workspace = true
